import { SectionCard } from "@/components/shared/SectionCard";

export default function SettingsPage() {
  return (
    <SectionCard title="Agency settings" eyebrow="Deferred">
      <p className="text-sm text-slate-600">Agency settings, members, branding, and billing controls are intentionally shell-only in this baseline.</p>
    </SectionCard>
  );
}
