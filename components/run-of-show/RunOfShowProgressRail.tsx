import type { RunOfShowProgressSnapshot } from "@/types/runOfShowLive";
export function RunOfShowProgressRail({ snapshot }: { snapshot: RunOfShowProgressSnapshot }) { return <div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-medium text-slate-500">Run of Show progress</p><h2 className="mt-1 text-xl font-semibold text-slate-950">{snapshot.completedCount}/{snapshot.totalCount} complete · {snapshot.progressPercent}%</h2><div className="mt-4 h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-slate-950" style={{ width: `${snapshot.progressPercent}%` }} /></div><div className="mt-4 grid gap-3 md:grid-cols-3">{[snapshot.previousSegment,snapshot.currentSegment,...snapshot.upcomingSegments].filter(Boolean).map((s)=><div key={s!.id} className="rounded-2xl border border-slate-200 p-3"><p className="text-xs uppercase text-slate-500">{s!.liveStatus}</p><p className="font-semibold">{s!.publicTitle}</p><p className="text-xs text-slate-500">{s!.room}</p></div>)}</div>{snapshot.producerWarning ? <p className="mt-4 rounded-2xl bg-amber-50 p-3 text-sm text-amber-900">{snapshot.producerWarning}</p> : null}</div>; }
