import { getSpeakerGreenRoomSnapshot } from "@/services/speaker-ops";
import { TeleprompterVersionPanel } from "./TeleprompterVersionPanel";
export function SpeakerTeleprompterPanel({ eventId, speakerId = "speaker-drake" }: { eventId:string; speakerId?:string }){ const snapshot=getSpeakerGreenRoomSnapshot(eventId,speakerId); const script=snapshot.approvedScriptVersion; return <div className="space-y-6"><TeleprompterVersionPanel approved={snapshot.approvedScriptVersion} pending={snapshot.pendingScriptVersion}/><div className="rounded-3xl bg-slate-950 p-6 text-white"><p className="text-sm uppercase tracking-wide text-slate-400">Teleprompter</p><h1 className="mt-2 text-3xl font-semibold">{script?.title??"No approved script"}</h1><div className="mt-8 rounded-3xl bg-black p-8 text-3xl leading-relaxed">{script?.scriptText??"No approved script has been locked."}</div></div></div> }
