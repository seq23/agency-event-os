import { getLastMinuteChanges } from "@/services/change-control";
import { SectionCard } from "@/components/shared/SectionCard";
export function LiveChangeAlertsPanel({eventId}:{eventId:string}){const changes=getLastMinuteChanges(eventId).filter(c=>!["approved","rejected","archived"].includes(c.status));return <SectionCard title="Live change alerts" eyebrow="Next segments at risk"><div className="space-y-3">{changes.map(c=><div key={c.id} className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-amber-950"><p className="font-semibold">{c.title}</p><p className="mt-1 text-sm">{c.recommendedAction}</p></div>)}</div></SectionCard>}
