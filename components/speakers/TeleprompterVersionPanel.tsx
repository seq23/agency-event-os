import type { SpeakerScriptVersion } from "@/types/speakerOps";
import { StatusBadge } from "@/components/shared/StatusBadge";
export function TeleprompterVersionPanel({ approved, pending }: { approved?: SpeakerScriptVersion; pending?: SpeakerScriptVersion }){return <div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">Teleprompter versions</p><div className="mt-4 grid gap-3 md:grid-cols-2"><div className="rounded-2xl bg-emerald-50 p-4"><p className="text-sm font-semibold text-emerald-800">Live approved version</p><p className="font-semibold">{approved?.title??"No live script"}</p><p className="text-sm text-slate-600">{approved?`v${approved.versionNumber}`:"Producer approval required"}</p></div><div className="rounded-2xl bg-amber-50 p-4"><p className="text-sm font-semibold text-amber-800">Pending change</p><p className="font-semibold">{pending?.title??"No pending script"}</p>{pending?<StatusBadge status={pending.status} tone="warn"/>:null}</div></div></div>}
