import fs from"node:fs";const r=["scripts/auth-state/common.sh","scripts/auth-state/status.sh","scripts/auth-state/backup.sh","scripts/auth-state/restore.sh","scripts/auth-state/remove-local.sh","scripts/auth-state/run-hallmark.sh","AUTH_STATE_VAULT.md"],m=r.filter(x=>!fs.existsSync(x));if(m.length)process.exit(1);if(!fs.readFileSync(".gitignore","utf8").includes(".auth"))process.exit(1);console.log("auth-state vault contract: PASS");
