import { describe, expect, it } from "vitest";
import { getSponsorFulfillmentSummary, getSponsorPackage, getSponsorReadyRoomSnapshot } from "@/services/sponsor-ops";
describe("sponsor ops",()=>{it("returns sponsor package deliverables",()=>{const p=getSponsorPackage("event-summit");expect(p.tierName).toBe("Gold Sponsor");expect(p.deliverables.length).toBeGreaterThan(0);});it("returns ready room snapshot",()=>{const s=getSponsorReadyRoomSnapshot("event-summit");const sum=getSponsorFulfillmentSummary("event-summit");expect(s.sponsorName).toBe("Clarity AI");expect(sum.totalDeliverables).toBeGreaterThan(0);});});
