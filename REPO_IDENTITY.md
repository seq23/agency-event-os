# REPO IDENTITY

Repository slug: `agency-event-os`
Primary branch: `main`
Artifact mode: full baseline snapshot
Deployment target: Cloudflare Workers / OpenNext via Wrangler
Package manager: npm (`package-lock.json`)

This file is the repo-local identity anchor for snapshot packaging, updater routing, validation reporting, and deployment/runbook references.

Wrong-repo execution is a hard stop. Before local updater use, confirm the local repository path, branch, remote, and dirty status.
