import { getSponsorPackage } from "@/services/sponsor-ops";
import { SectionCard } from "@/components/shared/SectionCard";
import { StatusBadge } from "@/components/shared/StatusBadge";
export function SponsorPackageOverview({eventId,sponsorId="sponsor-clarity"}:{eventId:string;sponsorId?:string}){const pkg=getSponsorPackage(eventId,sponsorId);return <SectionCard title={pkg.tierName} eyebrow="Sponsor package"><div className="space-y-3">{pkg.deliverables.map(item=><div key={item.id} className="flex items-center justify-between rounded-2xl border border-slate-200 p-4"><div><p className="font-semibold">{item.label}</p><p className="text-sm text-slate-500">Client approval {item.clientApprovalRequired?"required":"not required"}</p></div><StatusBadge status={item.status} tone={["approved","locked"].includes(item.status)?"good":"warn"}/></div>)}</div></SectionCard>}
