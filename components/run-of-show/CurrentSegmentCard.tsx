import type { LiveRunOfShowSegment, RunOfShowVisibilityProfile } from "@/types/runOfShowLive";
import { StatusBadge } from "@/components/shared/StatusBadge";
export function CurrentSegmentCard({ segment, visibility }: { segment?: LiveRunOfShowSegment; visibility: RunOfShowVisibilityProfile }) { if(!segment)return <div className="rounded-3xl border border-slate-200 bg-white p-5">No active segment</div>; return <div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex justify-between"><div><p className="text-sm text-slate-500">Current live segment</p><h2 className="text-2xl font-semibold">{segment.publicTitle}</h2><p className="text-sm text-slate-500">{segment.room} · {segment.durationMinutes} min</p></div><StatusBadge status={segment.liveStatus} tone={segment.liveStatus === "current" ? "good" : "neutral"}/></div><p className="mt-4 text-slate-600">{segment.clientFacingDescription}</p>{visibility.canSeeTechnicalCues ? <p className="mt-4 rounded-2xl bg-slate-50 p-3 text-sm">{segment.technicalCues}</p> : null}</div>; }
