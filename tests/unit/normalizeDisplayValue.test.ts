import{describe,it,expect}from'vitest';import{normalizeDisplayValue}from'../../lib/presentation/normalizeDisplayValue';import corpus from'../fixtures/production-shaped-payloads.json';describe('normalizeDisplayValue',()=>{it('sanitizes',()=>expect(normalizeDisplayValue('<script>x</script><b>A &amp; B</b>').summary).toBe('A & B'));it('corpus',()=>{for(const value of Object.values(corpus)){const r=normalizeDisplayValue(value,320);expect(typeof r.summary).toBe('string');expect(r.summary.length).toBeLessThanOrEqual(320)}})});
